{
  "api": {
    "db": { 
      "jdbcUrl": "$EXCHANGE_DB_URL",
      "user": "$EXCHANGE_DB_USER",
      "password": "$EXCHANGE_DB_PW"
    },
    "root": { 
      "password": "$EXCHANGE_ROOT_PW"
    },
    "tls": {
      "truststore": "$EXCHANGE_TRUST",
      "password": "$EXCHANGE_TRUST_PW"
    }
  }
}
